class Solution {
    public int findKthNumber(int n, int k) {
        List<String> temp = new ArrayList<>();
        for (int i = 1; i <= n; i++) {
            temp.add(String.valueOf(i));
        }
        Collections.sort(temp);
         int num = Integer.parseInt(temp.get(k-1));
         return num;
    }
}
