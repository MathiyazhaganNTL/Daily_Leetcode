public class Solution {
    // You need to treat n as an unsigned value
    public int reverseBits(int n) {
        // Store the reversed bits
        int reversed = Integer.reverse(n);
        return reversed;
    }
}
